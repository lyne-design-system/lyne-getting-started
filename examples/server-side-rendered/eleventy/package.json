{
  "name": "eleventy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clear:components": "rm -rf dist && mkdir dist && cd dist && mkdir js",
    "copy:components": "npm run clear:components && ncp ./node_modules/lyne-test/ ./dist/js/lyne-test/",
    "dev": "eleventy --serve",
    "start": "npm run copy:components & npm run dev",
    "build": "npm run copy:components & eleventy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "~0.12.1",
    "ncp": "~2.0.0",
    "netlify-cli": "~4.0.2"
  },
  "dependencies": {
    "lyne-test": "~9.20.1"
  }
}
